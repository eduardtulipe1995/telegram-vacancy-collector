# Telegram Vacancy Collector Bot

Автоматический бот для сбора вакансий из 105 Telegram каналов и отправки их пользователю @mediaya.

## Функциональность

- Автоматический сбор вакансий каждый день в 21:00 МСК
- Фильтрация по позициям: Сценарист, Редактор (видео), Шеф-редактор (видео)
- Дедупликация вакансий
- Отправка уведомлений в Telegram

## Архитектура

```
Render Background Worker
├── Pyrogram (чтение каналов через User Account)
├── python-telegram-bot (отправка уведомлений)
├── PostgreSQL (хранение вакансий)
└── APScheduler (запуск в 21:00 МСК)
```

## Установка и настройка

### 1. Получение Telegram API Credentials

#### API ID и API Hash (для User Bot)

1. Перейдите на https://my.telegram.org
2. Войдите с вашим номером телефона
3. Выберите "API Development Tools"
4. Создайте новое приложение:
   - App title: Vacancy Collector
   - Short name: vacancy_bot
   - Platform: Other
5. Сохраните `api_id` и `api_hash`

#### Bot Token (для отправки уведомлений)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Укажите имя: Vacancy Notification Bot
4. Укажите username (должен быть уникальным)
5. Сохраните полученный `BOT_TOKEN`

#### Генерация Session String

Локально выполните этот скрипт для генерации session string:

```python
# generate_session.py
from pyrogram import Client

API_ID = "your_api_id"
API_HASH = "your_api_hash"
PHONE_NUMBER = "+79123456789"

with Client("my_account", api_id=API_ID, api_hash=API_HASH, phone_number=PHONE_NUMBER) as app:
    print(f"Session String: {app.export_session_string()}")
```

Запустите:
```bash
pip install pyrogram TgCrypto
python generate_session.py
```

Сохраните полученный session string.

### 2. Настройка пользователя @mediaya

**ВАЖНО:** Пользователь @mediaya должен один раз написать боту `/start`. Это нужно сделать только ОДИН РАЗ после деплоя. После этого вакансии будут приходить автоматически каждый день без дополнительных действий.

### 3. Локальное тестирование (опционально)

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd telegram_jobs

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл (скопируйте из .env.example)
cp .env.example .env

# Заполнить .env файл вашими credentials

# Запустить тестовый сбор вакансий
python main.py --test
```

## Деплой на Render

### 1. Создание GitHub репозитория

```bash
git init
git add .
git commit -m "Initial commit: Telegram vacancy collector bot"
git branch -M main
git remote add origin <your-github-repo-url>
git push -u origin main
```

### 2. Создание PostgreSQL на Render

1. Перейдите на https://dashboard.render.com
2. New → PostgreSQL
3. Name: `telegram-jobs-db`
4. Plan: Free
5. Create Database
6. Сохраните `Internal Database URL` (понадобится для переменных окружения)

### 3. Создание Background Worker на Render

1. New → Background Worker
2. Connect your GitHub repository
3. Name: `telegram-jobs-worker`
4. Runtime: Python 3
5. Build Command: `pip install -r requirements.txt`
6. Start Command: `python main.py`
7. Plan: Free

### 4. Настройка Environment Variables

В настройках Background Worker добавьте следующие переменные:

```
API_ID=<your_api_id>
API_HASH=<your_api_hash>
SESSION_STRING=<your_base64_session_string>
BOT_TOKEN=<your_bot_token>
TARGET_USERNAME=mediaya
DATABASE_URL=<postgres_internal_url_from_step_2>
SCHEDULE_TIME=21:00
TIMEZONE=Europe/Moscow
LOG_LEVEL=INFO
```

### 5. Деплой

1. Нажмите "Create Background Worker"
2. Render автоматически:
   - Клонирует ваш репозиторий
   - Установит зависимости
   - Создаст таблицы в БД
   - Загрузит 105 каналов из CSV
   - Запустит бота

### 6. Проверка

1. Проверьте логи в Render Dashboard
2. Убедитесь что бот запустился без ошибок
3. Отправьте `/start` боту от @mediaya
4. Дождитесь 21:00 МСК или запустите тестовый сбор

## Мониторинг

### Просмотр логов

В Render Dashboard → вкладка Logs можно увидеть:
- Статус запуска бота
- Количество прочитанных сообщений
- Количество найденных и отправленных вакансий
- Ошибки (если есть)

### Ключевые метрики

- Количество обработанных каналов
- Количество найденных вакансий (до фильтрации)
- Количество отфильтрованных вакансий
- Количество дубликатов
- Количество отправленных вакансий

## Формат вывода

Вакансии отправляются в формате:

```
🎬 Новые вакансии за последние 24 часа:

1. Видеоредактор - Студия "Креатив" - https://t.me/normrabota/12345

2. Сценарист для YouTube - VideoLab - https://t.me/workasap/67890

3. Шеф-редактор видеопродакшн - MediaPro - https://t.me/mediajobs_ru/11223

Всего найдено: 3 вакансий
```

Если вакансий нет, отправляется: "Вакансий не найдено"

## Техническая информация

### Фильтрация вакансий

**Сценарист:**
- Keywords: сценарист, screenwriter, копирайтер-сценарист
- Видео-контекст: не требуется

**Редактор (только видео):**
- Keywords: редактор видео, видеоредактор, video editor, монтажер
- Exclude: редактор текста, литературный редактор, книжный редактор
- Видео-контекст: ОБЯЗАТЕЛЕН (минимум 2 упоминания: видео, монтаж, ролик, съёмка и т.д.)

**Шеф-редактор (только видео):**
- Keywords: шеф-редактор, главный редактор
- Exclude: книжное издательство, медиа, журнал, газета
- Видео-контекст: ОБЯЗАТЕЛЕН

### Дедупликация

- SHA-256 хеш от (название + компания + URL)
- Fuzzy matching с порогом 90%
- Временное окно: 7 дней

### Rate Limiting

- Глобальная задержка: 100ms между запросами
- Задержка на канал: 500ms
- Батчинг: 10 каналов одновременно
- Пауза между батчами: 30 секунд

## Troubleshooting

### Бот не отправляет сообщения

**Причина:** @mediaya не написал боту `/start`

**Решение:** Отправьте `/start` боту один раз

### FloodWait ошибки

**Причина:** Превышен rate limit Telegram API

**Решение:** Бот автоматически обрабатывает FloodWait и ждёт указанное время

### Нет вакансий в ежедневной рассылке

**Причины:**
- За последние 24 часа не было подходящих вакансий
- Все найденные вакансии - дубликаты
- Фильтры слишком строгие

**Решение:** Проверьте логи для диагностики

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Убедитесь что все environment variables настроены
3. Проверьте что @mediaya отправил `/start` боту
4. Проверьте что PostgreSQL database запущена

## Лицензия

MIT
