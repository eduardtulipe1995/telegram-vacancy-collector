# 🚀 Быстрый старт на ЗАВТРА

## Проблема сегодня
Telegram блокирует отправку кодов после множественных запросов.
**Подождите 12-24 часа** - это стандартная защита Telegram от спама.

## Что делать ЗАВТРА УТРОМ

### Шаг 1: Генерация SESSION_STRING (5 минут)

Откройте терминал:

```bash
cd /Users/eduardepstejn/claude_code/telegram_jobs
source venv/bin/activate
python generate_session_interactive.py
```

**ВАЖНО: Попробуйте с ДРУГОЙ СЕТИ:**
- Включите **мобильный интернет** (раздайте с телефона)
- Или попробуйте в **другом месте** (кафе, работа)
- Telegram может блокировать по IP-адресу

Введите:
1. Номер телефона: **+79261282279** (или любой другой доступный)
2. Код из Telegram (должен прийти!)
3. Пароль 2FA (если есть)

SESSION_STRING автоматически сохранится в `.env`

### Шаг 2: Проверка (1 минута)

```bash
python verify_setup.py
```

Должны быть все ✅

### Шаг 3: Тестовый запуск (2 минуты)

```bash
python main.py --test
```

Бот:
- Подключится к Telegram
- Прочитает 105 каналов
- Найдет вакансии
- Отправит их @mediaya и @eduardtulipe

### Шаг 4: Деплой на Render (10 минут)

Если тест прошел успешно:

```bash
git push origin main
```

Затем:
1. Создайте **PostgreSQL базу** на Render.com
2. Создайте **Background Worker** на Render.com
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения (из .env, но DATABASE_URL - PostgreSQL URL)
5. Deploy!

---

## Альтернатива: Использовать ДРУГОЙ номер

Если ваш номер **+79261282279** всё равно не получает коды:

### Вариант 1: Попросите друга/родственника
- Временно используйте их номер для SESSION_STRING
- Это НЕ влияет на то, кому приходят вакансии
- Вакансии всё равно придут @mediaya и @eduardtulipe

### Вариант 2: Виртуальный номер
- Можно купить временный номер на сервисах типа:
  - sms-activate.org
  - onlinesim.ru
- Стоит ~20-50 рублей
- Используйте только для генерации SESSION_STRING

---

## Если коды ВСЕГО РАВНО не приходят завтра

Попробуйте через **QR-код авторизацию**:

```bash
python generate_with_qr.py
```

(Скрипт будет создан позже если понадобится)

---

## Контакты для помощи

Если что-то не работает - напишите мне с:
- Скриншотом ошибки из терминала
- Описанием на каком шаге застряли

Всё будет работать! 🎉
